export const openPomodoroStyles = /* css */ `
.openpomodoro {
  position: relative;
  background-color: var(--crimsonred);
  overflow: hidden;
  color: var(--white);
}

.openpomodoro--started {
}

.openpomodoro--notstarted {
	filter: grayscale(80%);
}

` // see the small character at the start of this line here? I can't.
